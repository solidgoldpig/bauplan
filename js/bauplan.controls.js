define([
        "default.control"
    ], function () {
/**
 * @module bauplan%controls
 * @description ## Controls loader
 * Loads defined controls
 *
 *     require("bauplan.controls");
 * 
 * Currently hard-coded to load just the default controls {@link module:default%control}
 */
    return;
});