{{!--
/**
 * @template textarea%edit
 * @language handlebars
 * @block control
 * @param {object} [attributes] Attributes to pass to el-textarea
 * @description Template used by textarea control in edit mode
 * 
 *     {{{template "textarea.edit" attributes=attributes}}}
 *
 * NB. It is usually preferable to use {@link template:control%view} rather than calling this directly
 *
 * @see template:control%view
 * @see template:control
 * @see template:control%edit
 */
--}}
{{{el-textarea attributes=attributes}}}