{{!--
/**
 * @template hidden%edit
 * @language handlebars
 * @block control
 * @param {object} [attributes] Attributes to pass to el-hidden
 * @description Template used by hidden control in edit mode
 * 
 *     {{{template "hidden.edit" attributes=attributes}}}
 *
 * NB. It is usually preferable to use {@link template:control%view} rather than calling this directly
 *
 * @see template:control%view
 * @see template:control
 * @see template:control%edit
 */
--}}
{{{el-hidden attributes=attributes}}}